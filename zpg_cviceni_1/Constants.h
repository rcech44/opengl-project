#pragma once

#define WINDOW_WIDTH 1000
#define WINDOW_HEIGHT 800

enum Direction {
    none = 0,
    forward = 1,
    backward = -1,
    left = -1,
    right = 1,
    up = 2,
    down = 3
};